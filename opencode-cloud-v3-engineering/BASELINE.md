# 基线、版本与路线切换

## 1. 本轮最新决定

用户明确要求：**将共享受限业务助手版 v3 搁置，v3 回到“让工程师更愿意把真正的工作交给它”**。

本包是当前有效的 v3 实施依据，目录使用 `opencode-cloud-v3-engineering/` 防止与旧包混淆，不改变产品版本号。

| 保留 | 搁置 | 禁止顺手开展 |
|---|---|---|
| 单用户独立完整 OpenCode、原生会话和工具、v2 按需运行 | Shared Agent、双模式权限、共享会话归属、新业务工具身份层、EDM/VAVE 等迁移 | Shared Pool、多机、通用插件／工作流／评测平台 |

旧提案的有用思想不等于新待办；不得将本包与其合并成一个更大版本。旧包和历史文档保留为历史材料，实际仓库文件的标记修改须保留历史、只修正入口说明。

## 2. 已接受事实

用户已确认 v0、v1、v2 完成。本轮读到的仓库参考 HEAD：

- `883659621264f7943758c022f0a35718ada1ac64`
- 提交说明：`Record user acceptance of WorkBench v2`
- v2 状态：`DELIVERED`，已记录本机用户签收。
- v2 实现提交：`cba249137c43c4b68162bf5bddf6ccf3f6b338d1`
- v2 技术验收提交：`f2b682467fef494c4fec6440cd95e0964c0c5d42`
- v2 记录的制品：`sha256:07b02dd95ddc77d3f1ee8c61ec26ab9f395e916d806aeca29c53cef6154a4153`

v2 已记录按需进入、三空间两名额、共享模型网关、停止重进、原生会话续接、ZIP 与补丁干净复现。此处继承其签收，不声称本轮重新运行这些测试。来源见 [SOURCES](SOURCES.md)。

## 3. 实际工作副本优先

Codex 开始时记录实际 HEAD、工作树和已有实现。上述 SHA 是参考快照，**不是要求强制 checkout、reset、回退或重新实施已修复功能**。

已有相关能力可直接复用并回归。旧 README/STATUS 中的历史 `BLOCKED` 不推翻新的用户签收。v2 的“本机已交付”不等于其他服务器现时在线。

先记录构建中的 OpenCode、UI、镜像、模型路由和配置摘要。参考实现使用 OpenCode `1.18.31`；默认保持实际已验收固定组合，不追踪 `latest`。v2 记录中 Qwen 真实任务通过，Luna 发现可用但聊天服务曾返回 500；不得将发现成功当作聊天成功，也不自动换模型。

## 4. 数据和部署保护

保留用户项目、未提交修改、Git 索引、会话、额度、密钥、已交付制品及回退路径。不因 v3 编号更改 `WorkBench-v1`、Compose 名、卷名或历史证据路径。

默认在获授权的开发环境和隔离测试项目中实施。本地开发不等于重新引入用户电脑连接器。未经明确部署授权，不访问、恢复或修改已退役远程部署，不重启模型服务或无关容器。

## 5. 三种“基线”不得混淆

- **产品验收基线**：v0—v2 已接受，不重新打开。
- **能力对照基线**：让 v2 在这批新真实缺陷上运行，用于判断增强效果；这是新实验，不是否认旧验收。
- **源码输入基线**：每个测试项目的起始提交与未提交文件清单；新旧候选必须从相同输入开始。

缺少测试案例、模型权限或部署条件时，精准报告本轮阻塞，不追问已确认的旧验收，不用假数据冒充新能力。
