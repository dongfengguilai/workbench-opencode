网页已由平台的原生 PTY 启动在 http://127.0.0.1:5173，不要再次启动。先将 web/stats.test.mjs 改为 Node 标准库 node:test 和 node:assert/strict 的测试，保留全部14项已有断言和覆盖，不删或降低要求；单独运行 node --test /workspace/project/web/stats.test.mjs，不能用 echo 掩盖退出码。然后通过原生 Bash 调用固定 playwright-cli：先 --help，用 -s=wb-static open http://127.0.0.1:5173 --config /trusted/browser.json，取得实际 snapshot 并操作。验证新浏览器输入区初始为空；输入“你好 😀
世界	!”点击统计，真实 DOM 断言字符数9、词数4，空白输入统计词数0，清空按钮使输入为空。CLI eval 或 run-code 断言失败须 throw，成功返回 WB_BROWSER_ASSERTIONS_PASS；不能将 ### Error 或模型总结当作通过。保留成功页面 PNG 到 /workspace/project/.workbench-artifacts/ses_f507ead15ffeUbQJZrsiTj0FYz/static-result.png，最后关闭浏览器。不要读环境或 /trusted 内容，不安装浏览器/插件，不改网页逻辑来迁就错误测试。
