Implement the actual patch download core for this OpenCode Cloud V0 product. Read opencode-cloud/test/patch-export.test.mjs and relevant product instructions first. Create opencode-cloud/src/patch-export.ts exporting async exportPatch({directory, baseline}) returning {baseline, files, patch}. The baseline must be a full commit SHA matching repository HEAD; fail explicitly if it differs. Export the final working tree relative to that baseline, including staged and unstaged modifications, deletion, untracked source and binary files. Keep the real Git index byte-for-byte unchanged, using a temporary index outside the repository. Exclude secrets (.env and credential/key files), state/database/cache/dependency paths even if tracked; obey gitignore for untracked files. Reject unsafe symlinks before staging. Use only Node standard library and git, never network or credentials. Run node --experimental-strip-types --test opencode-cloud/test/patch-export.test.mjs and explain actual results. Do not modify tests, commit, delete existing code or inspect runtime secrets. This is a real product feature; write the implementation yourself.
