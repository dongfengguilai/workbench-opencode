继续同一项目，验证第二种交付类型 Vite/React。先调用原生 question 向我确认，选项“保留静态版并新增React”，等我的实际确认后再改。只能修改或新增本轮你创建的 web 文件，不触碰任何之前的用户文件。保留当前静态网页为 web/static.html，并保留 stats.mjs 和标准库全部14项断言；将 web/index.html 改为 React 挂载页，使用 React/ReactDOM 18.3.1、Vite 7.3.6，生成并保留 package-lock.json。界面标题“文本统计器 · React”，保留输入文本、统计、清空和码点/空白计数功能，增加“示例文本”按钮，填入“你好 😀
世界	!”供浏览器操作验证。依赖只从公开 HTTPS 官方 npm registry 安装，不安装插件、MCP、浏览器或新的模型配置，不读取环境变量、密钥或 /trusted 内容。package.json 提供 dev、build、test 脚本。实际运行 npm run build 和 npm test；Bash 命令不要附 echo 掩盖退出码，依赖安装可 --ignore-scripts。原预览在镜像更新时已停止，不要自己另启动；完成代码、构建和测试后报告，等待我从平台启动浏览器验证。
