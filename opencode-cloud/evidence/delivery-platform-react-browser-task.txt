React 预览已由平台本人原生 PTY 启动 http://127.0.0.1:5173。我的真实内置浏览器看到页面空白，不能算通过。先通过原生 Bash 的固定 playwright-cli -s=wb-react open http://127.0.0.1:5173 --config /trusted/browser.json，并用 snapshot/console 保存真实错误，再只修本轮 web React 源码使页面挂载；禁止改 /trusted、安装浏览器、插件或改工具来绕过问题。还需修 package.json test 相对路径：npm 在 web 目录运行，脚本必须是 node --test stats.test.mjs src/__tests__/App.test.mjs，原有14项与新增15项都要实际执行，不能用15PASS代替29项；保留所有原断言。示例文本按钮请填入包含换行和Tab的中文/emoji组合，点击统计应得到字符数9、单词数4。修好后实际 npm run build 和 npm test（独立命令，不附echo）。随后通过同一个 CLI 浏览器真实填写/点击，建议 run-code 完成：断言页面标题挂载，点击示例文本、统计并断言9/4，空白输入统计词数0，清空输入为空；失败 throw，全部通过返回 WB_BROWSER_ASSERTIONS_PASS。保存成功页面 PNG 到 /workspace/project/.workbench-artifacts/ses_f507ead15ffeUbQJZrsiTj0FYz/react-result.png，最后 close。不要自己另启动预览、不读环境密钥、不改其他用户文件。
