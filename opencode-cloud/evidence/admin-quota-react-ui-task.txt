补充验收：上一轮 eval 的9/4与空白0重新计算了textarea输入，不是对实际界面输出的断言。只补这一个证据阻塞，禁止改源码和测试。实际通过固定CLI --config /trusted/browser.json open后，使用 run-code 的 page.getByRole 点击示例文本、统计，然后读取 page.locator('.result').innerText() 并断言其中字符数：9、单词数：4（允许空白）；填入纯空白再统计，断言 .result 的实际输出单词数：0；点击清空，断言textarea inputValue为空且 .result count为0。全部成功返回 WB_REACT_UI_ASSERTIONS_PASS，失败 throw。再次示例/统计后截图到 .workbench-artifacts/ses_f507ead15ffeUbQJZrsiTj0FYz/react-result-verified.png，最后关闭，不安装依赖，不读环境。
