WorkBench UI 独立代码验收。先查看项目结构与 git status，保留所有已有未提交修改，不运行 reset、clean、checkout、git add 或提交。只允许新增 opencode-cloud/src/ui-demo-initials.mjs 与 opencode-cloud/test/ui-demo-initials.test.mjs；任一文件已存在则停止。请先使用原生 question 工具询问我是否创建这两个文件，得到确认后再实现。导出 initials(input)：仅接受字符串，否则抛 TypeError；按空白拆分去除空项，取前两个词的第一个 Unicode 字符，转换大写后拼接，空字符串返回空字符串。仅用 Node 标准库 node:test 与 assert，覆盖 Ada Lovelace→AL、workbench→W、张 三→张三、空白、混合空白和非字符串。实际执行 node --test opencode-cloud/test/ui-demo-initials.test.mjs，不安装依赖、不修改其他文件。
