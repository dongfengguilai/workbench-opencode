Independent verification found that your implementation leaks UNTRACKED excluded paths: .env.production, src/private-key.pem and state/session.db appear in patch and files because you only restore excluded paths from baseline ls-tree. Fix this actual product defect in opencode-cloud/src/patch-export.ts yourself. The original tests remain unchanged. The maintainer has added the exact stronger independent regression at opencode-cloud/test/patch-export-extra.test.mjs; do not change either test file. Run node --experimental-strip-types --test opencode-cloud/test/patch-export.test.mjs opencode-cloud/test/patch-export-extra.test.mjs. Exclude both tracked and untracked secrets/state while preserving full legitimate source changes and the original index. Use literal path handling for file names; no network, credentials or commits. Report actual results.
 Also exclude DELETED tracked secret paths by restoring their baseline entries; the stronger regression has two cases now. Do not merely claim the tests pass: run the original and extra tests, inspect failures, and fix source only.
