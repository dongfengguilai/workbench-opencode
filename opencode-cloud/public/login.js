async function clearStorage(){localStorage.clear();sessionStorage.clear();for(const db of await indexedDB.databases())if(db.name)indexedDB.deleteDatabase(db.name);for(const name of await caches.keys())await caches.delete(name);for(const worker of await navigator.serviceWorker.getRegistrations())await worker.unregister();}
document.querySelector('#login').addEventListener('submit',async event=>{event.preventDefault();const form=event.currentTarget;const button=form.querySelector('button');button.disabled=true;document.querySelector('#error').textContent='';try{await clearStorage();const data=Object.fromEntries(new FormData(form));const response=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});if(!response.ok)throw new Error('登录失败，请检查账号或服务状态。');location.replace('/');}catch(e){document.querySelector('#error').textContent=e.message;button.disabled=false;}});
