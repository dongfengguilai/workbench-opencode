async function clearStorage(){localStorage.clear();sessionStorage.clear();for(const db of await indexedDB.databases())if(db.name)indexedDB.deleteDatabase(db.name);for(const name of await caches.keys())await caches.delete(name);for(const worker of await navigator.serviceWorker.getRegistrations())await worker.unregister();}
document.querySelector('.theme').addEventListener('click',()=>window.workbenchTheme(document.documentElement.dataset.colorScheme==='dark'?'light':'dark'));
document.querySelector('#login').addEventListener('submit',async event=>{event.preventDefault();const form=event.currentTarget;const button=form.querySelector('button[type=submit]');if(button.disabled)return;const label=button.innerHTML;button.disabled=true;button.textContent='正在登录…';form.setAttribute('aria-busy','true');document.querySelector('#error').textContent='';try{await clearStorage();const data=Object.fromEntries(new FormData(form));const response=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});if(!response.ok)throw new Error(response.status===401?'账号或密码错误，请重新输入。':'登录服务暂不可用，请稍后再试。');location.replace('/');}catch(e){document.querySelector('#error').textContent=e.message;button.disabled=false;button.innerHTML=label;form.removeAttribute('aria-busy');}});
