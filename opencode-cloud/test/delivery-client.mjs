import http from 'node:http';import {readFileSync} from 'node:fs';
export const config=JSON.parse(readFileSync(new URL('../runtime/platform.json',import.meta.url)));
export function request(path,{method='GET',cookie,body,preview=false,headers={}}={}){return new Promise((resolve,reject)=>{const origin=preview?'http://localhost:8445':'http://127.0.0.1:8444';const r=http.request({hostname:'127.0.0.1',port:preview?8445:8444,path,method,headers:{Host:new URL(origin).host,Origin:origin,...(cookie?{Cookie:cookie}:{}),...(body?{'Content-Type':'application/json'}:{}),...headers}},res=>{const chunks=[];res.on('data',b=>chunks.push(b));res.on('end',()=>{const buffer=Buffer.concat(chunks);resolve({status:res.statusCode,headers:res.headers,buffer,text:buffer.toString(),json:()=>JSON.parse(buffer.toString())})});});r.on('error',reject);r.setTimeout(65000,()=>r.destroy(Error('Real platform response timeout')));r.end(body?JSON.stringify(body):undefined);});}
export async function login(name='admin'){for(let i=0;i<50;i++){if((await request('/__platform/login')).status===200)break;if(i===49)throw Error('Platform login page is not ready');await new Promise(r=>setTimeout(r,100))}const r=await request('/api/auth/login',{method:'POST',body:{username:name,password:config.users[name].password}});if(r.status!==200)throw Error('Real login failed '+r.status);return r.headers['set-cookie'][0].split(';')[0];}
