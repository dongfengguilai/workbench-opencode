import tls from 'node:tls';import http from 'node:http';import {readFileSync,writeFileSync} from 'node:fs';import {randomBytes} from 'node:crypto';import assert from 'node:assert/strict';import {request,login,config} from './delivery-client.mjs';
const result={status:'IN_PROGRESS',checks:[]};const u=new URL(config.origin);let c,socket;
try{
 for(let i=0;i<24;i++)await new Promise(resolve=>{const s=tls.connect({host:u.hostname,port:u.port,ca:readFileSync('runtime/tls.crt')});s.on('error',()=>resolve());s.once('secureConnect',()=>{s.write(`GET /__preview/ HTTP/1.1\r\nHost: ${u.host}\r\nOrigin: ${u.origin}\r\nConnection: Upgrade\r\nUpgrade: websocket\r\nSec-WebSocket-Version: 13\r\nSec-WebSocket-Key: ${randomBytes(16).toString('base64')}\r\n\r\n`);setTimeout(()=>s.destroy(),2)});s.once('close',resolve)});
 assert.equal((await request('/__platform/login')).status,200);result.checks.push('24 real verified TLS upgrade/disconnects do not terminate platform');
 c=await login();const t=(await request('/__platform/preview/ticket',{method:'POST',cookie:c})).json().url;const claim=await request(new URL(t).pathname+new URL(t).search,{preview:true});assert.equal(claim.status,303);const pc=claim.headers['set-cookie'][0].split(';')[0];
 const client=await request('/@vite/client',{preview:true,cookie:pc});assert.equal(client.status,200);const token=client.text.match(/const wsToken = "([^"]+)"/)?.[1];assert.ok(token,'Actual Vite WS token missing');
 socket=await new Promise((resolve,reject)=>{const r=http.request({hostname:'127.0.0.1',port:8445,path:'/?token='+encodeURIComponent(token),headers:{Host:'localhost:8445',Origin:'http://localhost:8445',Cookie:pc,Connection:'Upgrade',Upgrade:'websocket','Sec-WebSocket-Version':'13','Sec-WebSocket-Key':randomBytes(16).toString('base64'),'Sec-WebSocket-Protocol':'vite-hmr'}});r.on('upgrade',(reply,s)=>{assert.equal(reply.statusCode,101);s.on('error',()=>{});s.resume();resolve(s)});r.on('response',r=>{r.resume();reject(Error('Actual Vite WS rejected '+r.statusCode))});r.on('error',reject);r.setTimeout(10000,()=>r.destroy(Error('WS timeout')));r.end()});
 result.checks.push('actual authenticated Vite WebSocket 101 through separate origin and own guard');
 const closed=new Promise((resolve,reject)=>{socket.once('close',resolve);setTimeout(()=>reject(Error('Logout failed to close actual preview WS')),5000).unref()});await request('/api/auth/logout',{method:'POST',cookie:c});await closed;assert.equal((await request('/',{preview:true,cookie:pc})).status,401);result.checks.push('logout closes established preview WS and revokes subsequent HTTP');
 result.status='PASS';console.log('PASS actual TLS disconnect, Vite WS and logout revoke');
}catch(e){result.status='FAIL';result.error=e.message;throw e;}finally{socket?.destroy();if(c)await request('/api/auth/logout',{method:'POST',cookie:c});writeFileSync('evidence/delivery-transport-live-result.json',JSON.stringify(result,null,2)+'\n')}
