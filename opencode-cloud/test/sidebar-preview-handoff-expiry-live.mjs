import {request,login} from './delivery-client.mjs';import assert from 'node:assert/strict';import {writeFileSync} from 'node:fs';
const cookie=await login();const r=await request('/',{cookie,headers:{Accept:'text/html'}});assert.equal(r.status,303);const url=new URL(r.headers.location),started=Date.now();await new Promise(r=>setTimeout(r,31000));const claim=await request(url.pathname+url.search,{headers:{Host:url.host,Origin:url.origin}});assert.equal(claim.status,401);await request('/api/auth/logout',{method:'POST',cookie});writeFileSync('evidence/sidebar-preview-handoff-expiry-live-result.json',JSON.stringify({status:'PASS',kind:'real_unchanged_clock_30_second_handoff_expiry',elapsedMs:Date.now()-started,rejectedStatus:claim.status},null,2)+'\n');
