import{readFileSync,writeFileSync,unlinkSync,existsSync,chmodSync}from'node:fs';import{login,request}from'./delivery-client.mjs';import assert from'node:assert/strict';
const saved='runtime/sidebar-preview-gui-index-backup',file='runtime/project/web/index.html',action=process.argv[2];const cookie=await login('trial-b');try{assert.deepEqual((await request('/session/status',{cookie})).json(),{});if(action==='unhealthy'){assert.equal(existsSync(saved),false);writeFileSync(saved,readFileSync(file),{mode:0o600});assert.ok((await request('/__platform/preview/status',{cookie})).json().pty);unlinkSync(file);assert.equal((await request('/__platform/preview/status',{cookie})).json().running,false);console.log('Owned Native process present; actual application unavailable');}else if(action==='restore'){writeFileSync(file,readFileSync(saved));chmodSync(file,0o644);unlinkSync(saved);console.log('Original source bytes restored; no service implicitly started');}else throw Error('Specify unhealthy|restore');}finally{await request('/api/auth/logout',{method:'POST',cookie});}
