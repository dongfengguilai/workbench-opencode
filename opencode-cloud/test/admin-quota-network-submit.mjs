import {login,request} from './delivery-client.mjs';import {writeFileSync} from 'node:fs';import {randomBytes} from 'node:crypto';
const sid='ses_f507ead15ffeUbQJZrsiTj0FYz',cookie=await login();if(Object.keys((await request('/session/status',{cookie})).json()).length)throw Error('Native busy');const messageID='msg_'+Date.now().toString(16)+randomBytes(6).toString('hex');
const text=`维护者已部署仅增加 npm 限连接默认值的候选镜像。只读回归现有网页，禁止修改源码、安装依赖、读取环境变量或 /trusted 内容。经原生 Bash：1. cd /workspace/project/web && npm config get maxsockets && npm config get fetch-retries && npm test，应为2、0、29测试通过。2. 固定 playwright-cli -s=wb-net open https://example.com --config /trusted/browser.json，然后snapshot。3. goto 'https://httpbin.org/redirect-to?url=https%3A%2F%2Fexample.com'，snapshot必须证明重定向最终 example.com。4. 尝试goto https://169.254.169.254/ 和 https://192.168.142.130/，应得到原生浏览器网络拒绝，不能用ignoreHTTPSErrors伪装。5. close。另使用原生 webfetch 获取 https://wttr.in/Shanghai?format=3 和 https://bun.sh/docs/runtime/networking/fetch。如失败保留具体工具输出，不替换为模型总结。CLI run-code 的正确参数是单个 JavaScript 函数文本 async (page) => { ... }，不用多行shell特殊转义。最终报告实际结果。`;
writeFileSync('evidence/admin-quota-network-task.txt',text);const r=await request('/session/'+sid+'/prompt_async',{method:'POST',cookie,body:{messageID,model:{providerID:'approved',modelID:'Qwen3.6-35B-A3B'},parts:[{type:'text',text}]}});writeFileSync('evidence/admin-quota-network-submission.json',JSON.stringify({session:sid,messageID,status:r.status,at:new Date().toISOString()}));if(r.status!==204)throw Error('Native submit '+r.status);await request('/api/auth/logout',{method:'POST',cookie});
