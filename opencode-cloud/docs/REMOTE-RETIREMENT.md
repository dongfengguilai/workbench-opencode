# 远程试验环境退役

2026-09-18：服务器 `10.243.117.57` 的 WorkBench 试验环境已按用户要求退役。用户确认内网登录可用，此反馈不代表全部验收或 A10 签收。后续默认在本机开发，到下一个大版本另行明确部署要求后再开展远程试验。

## 当前资源

- 本机源码、未提交参考图、GitHub 仓库和已有离线部署包保留。本轮只提交退役记录与证据，不修改产品代码。
- 旧远程工作台端口 8443／8447、预览端口 8445／8449 已关闭；安装目录 `/home/aisvr/mnt/sda/programs/WorkBench-v1` 已删除，旧启动命令不能再在那里运行。
- 工程师与管理员的全部试用数据已进入本机冷备。服务器 Qwen 模型及其他业务保持不变；共享 Node 镜像保留。
- 本轮未卸载用户电脑上的根证书，也未修改客户端 hosts、浏览器信任或宿主共享挂载设置。

## 私密冷备

目录：`/home/vmware/Workspace/projects/experiments/Workbench_space/releases/retired-workbench-v1/20260918T053225Z/`（0700）。

归档：`.WorkBench-v1-retirement-20260918T053225Z.tar.gz`（0600），2,303,035,000 字节。

SHA-256：`b282dccfc45a19b6786598d4e943b89ef0693b39df3d27f21e38440edce4a124`。

归档包含整个原安装目录，包括卸载后的两套原生磁盘镜像、试用项目与会话、截图、配置、私有 CA、制品和历史备份。它含受保护凭据，不能提交源码仓库或作为公开部署包分发。旁边保存原始清单、校验结果和维护脚本，均为私密文件。

校验已读取归档所有普通文件，核对完整压缩包摘要和两套原生磁盘镜像摘要；没有在服务器上恢复或重新启动。日后如需恢复，应先校验摘要、确认目标目录没有新数据与运行环境，并由维护者检查文件系统及限额挂载条件。不要把旧备份自动导入新大版本；新的远程试验默认使用该版本的全新部署。

原升级暂存包属于固定 RC3 制品，无 runtime 或业务备份，其摘要与本机 `workbench-v1-20260918-dual-identity-rc3.tar.gz` 一致；本机 RC3 包保留，远程暂存副本清除。

## 实际证据与历史说明

证据在 `opencode-cloud/evidence/retirement-*.json`。其他服务器业务的容器 ID、镜像、启动时间、运行状态、重启计数与挂载集合均前后一致。首次检查中止的记录也保留，未以历史功能证据冒充本轮成功。

之前的部署与使用说明继续保留，用于维护历史版本；其中的服务器地址现已失效，只有再次部署后才可使用。下一轮开发不默认操作远程服务器。
