# WorkBench 试用

入口：`https://192.168.142.130:8443`。当前为自签试用证书；请先向维护者核对证书指纹。管理员账号、密码在本机保护文件 `opencode-cloud/runtime/ADMIN_LOGIN.txt`，不要提交或粘贴到会话中。

在当前运行 Codex 的宿主机，可直接通过内置浏览器打开 `http://127.0.0.1:8444`，无需处理自签证书提示。该本机验证入口已经启动，重启命令见 OPERATIONS；其他设备不能通过这个回环地址访问本平台。本机页面后的代理仍验证真实 HTTPS 平台证书。

实际示例“新增 normalizeWhitespace 示例及测试”的原生会话地址：`http://127.0.0.1:8444/server/aHR0cDovLzEyNy4wLjAuMTo4NDQ0/session/ses_f52403502ffeaGvtr8uvfQZ8W5`。登录后可直接重新打开该地址；4 项测试已实际通过。Changes 下选择 Last turn changes 查看本次两个文件；顶部导出包含相对固定基线的全部可导出变更，也包含保留的既有项目修改。变更清单是实际 JSON 响应，浏览器返回后可继续原会话和下载。

1. 登录后默认进入“项目”，显示本人的授权项目与真实环境状态。点击“进入项目”或侧栏“新建会话”直接进入原生草稿，不再依赖旧首页项目缓存。
2. 输入代码需求。默认使用用户新批准的 Qwen3.6-35B-A3B；原生模型按钮中保留已批准的 Luna，可自行选择这两个固定模型。不能设置服务器或 Key。代码、提示词与工具输出发送到对应已批准的模型端点。
3. 查看原生流式消息和工具测试。原生提问出现时选择并提交，运行中点击“停止”；确认结果后再明确提出继续需求。
4. 用会话搜索切换历史会话。查看原生变更、文件；点击“终端”打开同一原生终端的底部面板。顶部“检查变更”显示固定基线的实际清单，“下载补丁”导出全部可导出变更，包括保留的既有修改。活动任务期间拒绝导出。
5. 当前工作副本固定基线为 9320d7bdc9f2f1e87c865646952c86f239a57d9b；在新的干净基线副本执行 `git apply --check workbench-opencode.patch`，再 `git apply workbench-opencode.patch`，并运行项目测试。下载不是自动提交或 push。
6. 刷新、注销重登后，原生会话与文件持续保存。浅／深主题首次跟随系统，点击主题按钮后记住选择；窄栏导航改为抽屉。

Qwen 真实代码示例：`http://127.0.0.1:8444/server/aHR0cDovLzEyNy4wLjAuMTo4NDQ0/session/ses_f51f0193effe59hdD5mXjOT6EO`。原生 Agent 创建 initials 函数及标准库测试，Unicode 边界修正后8项通过，终端和干净补丁副本总计15项通过。会话保留此前 Luna 外部 TLS 失败；查看后面的 Qwen 成功结果。已有旧会话若仍选中 Luna，可在输入区模型按钮切为 Qwen；切换只影响后续请求。
一个环境同时只接收一个顶层代码任务。重复消息 ID 返回 409；先查看原会话和状态，不能盲目重发。模型不可用显示真实错误，不自动切换或重试；由你检查结果后明确选择批准的模型并继续。环境不可用返回 503。进程中断后查看原生工具与错误状态，不能把中断当作成功，也不承诺任意外部副作用可续跑。

公开 HTTPS 网站（443 端口）已按用户批准开放，原生 Webfetch 可查询天气或读取公开文档。原天气会话中重新查询已真实成功：`http://127.0.0.1:8444/server/aHR0cDovLzEyNy4wLjAuMTo4NDQ0/session/ses_f5236b382ffeAgB48sLtafH7Qy`。历史 Failed 不会被删除；看后面的新工具结果。私网、宿主管理、元数据、非 443 目标及普通 HTTP 仍被拒绝。站点、DNS 或外部代理失败仍可能显示原生 Transport error，维护者按网关日志定位；不会把失败改成成功。CONNECT 是环境级 HTTPS 通道，不限制加密隧道内的方法，HTTPS 内容仍受原生证书验证。

旧会话地址兼容；新的项目首页与新建会话入口已复用原生项目和草稿流程。

范围：本项目的 Node 标准库/Git 工作流，一个固定模板、两个用户明确批准的固定模型、预先授权项目。没有本地文件访问、仓库写凭据、push/自动 PR、任意服务预览、环境配置或扩展安装。项目 opencode.json/.opencode、外部自动发现配置不加载；普通源码与自然语言需求仍可正常编辑和表达。密钥、状态、缓存和依赖不进入补丁。新增二进制支持 Git binary patch；当前项目无子模块，不承诺其他项目类型。

实际验收进度见 `opencode-cloud-v0/STATUS.md`。自动化证据不替代实际用户签收。
