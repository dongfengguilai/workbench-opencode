# OpenCode Cloud V0 试用

入口：`https://192.168.142.130:8443`。当前为自签试用证书；请先向维护者核对证书指纹。管理员账号、密码在本机保护文件 `opencode-cloud/runtime/ADMIN_LOGIN.txt`，不要提交或粘贴到会话中。

在当前运行 Codex 的宿主机，可直接通过内置浏览器打开 `http://127.0.0.1:8444`，无需处理自签证书提示。该本机验证入口已经启动，重启命令见 OPERATIONS；其他设备不能通过这个回环地址访问本平台。本机页面后的代理仍验证真实 HTTPS 平台证书。

实际示例“新增 normalizeWhitespace 示例及测试”的原生会话地址：`http://127.0.0.1:8444/server/aHR0cDovLzEyNy4wLjAuMTo4NDQ0/session/ses_f52403502ffeaGvtr8uvfQZ8W5`。登录后可直接重新打开该地址；4 项测试已实际通过。Changes 下选择 Last turn changes 查看本次两个文件；顶部导出包含相对固定基线的全部可导出变更，也包含保留的既有项目修改。变更清单是实际 JSON 响应，浏览器返回后可继续原会话和下载。

1. 登录，点击顶部“授权项目”。这里是本人的云端独立工作副本。
2. 在完整 OpenCode 原生页面输入代码需求。固定 Luna 模型，无需填写 Key、模型或服务器。代码、提示词和工具输出会发往用户批准的模型网关。
3. 查看流式回复、文件修改与工具测试输出；原生提问出现时选择并提交回答。需要停止时点击原生 Stop。完成后可继续补充需求。
4. 查看原生 Diff 和测试结果；顶部“变更清单”给出相对固定基线的文件清单，“下载变更”取得完整 Git 二进制补丁。活动任务时拒绝导出，请先完成或停止。
5. 当前验收工作副本基线为 9320d7bdc9f2f1e87c865646952c86f239a57d9b（远端保留该 commit）；先 checkout 该 commit，再在干净基线上执行 `git apply --check workbench-opencode.patch`，再 `git apply workbench-opencode.patch`，运行项目测试。
6. 刷新或重新登录后打开原会话。项目、原生 SQLite 会话及快照保存在独立持久目录；退出登录会断开该登录的流式连接，已经提交的任务保留在原生实例中。

一个环境同时只接收一个顶层代码任务。重复消息 ID 返回 409；先查看原会话和状态，不能盲目重发。模型不可用会显示真实错误，不切换模型。环境不可用返回 503。进程中断后查看原生工具与错误状态，不能把中断当作成功，也不承诺任意外部副作用可续跑。

公开 HTTPS 网站（443 端口）已按用户批准开放，原生 Webfetch 可查询天气或读取公开文档。原天气会话中重新查询已真实成功：`http://127.0.0.1:8444/server/aHR0cDovLzEyNy4wLjAuMTo4NDQ0/session/ses_f5236b382ffeAgB48sLtafH7Qy`。历史 Failed 不会被删除；看后面的新工具结果。私网、宿主管理、元数据、非 443 目标及普通 HTTP 仍被拒绝。站点、DNS 或外部代理失败仍可能显示原生 Transport error，维护者按网关日志定位；不会把失败改成成功。CONNECT 是环境级 HTTPS 通道，不限制加密隧道内的方法，HTTPS 内容仍受原生证书验证。

首次进入或首页没有项目时，先点顶部“授权项目”；首页 New session 依赖原生已打开项目列表。本次只修复网络访问，没有改首页。

范围：本项目的 Node 标准库/Git 工作流，一个固定模板、固定模型、预先授权项目。没有本地文件访问、仓库写凭据、push/自动 PR、任意服务预览、环境配置或扩展安装。项目 opencode.json/.opencode、外部自动发现配置不加载；普通源码与自然语言需求仍可正常编辑和表达。密钥、状态、缓存和依赖不进入补丁。新增二进制支持 Git binary patch；当前项目无子模块，不承诺其他项目类型。

实际验收进度见 `opencode-cloud-v0/STATUS.md`。自动化证据不替代实际用户签收。
